import React from "react";

/*
case using prisma
// import db from "@/utils/db";
// // async means that the function will always return a promise.
// // If the code has a return statement, the promise will be resolved with the value of the return statement.
// // If the code has no return statement, the promise will be resolved with undefined.
// // await is used to wait for a Promise. It can only be used inside an async function.
// // why doing crud with db need to use async and await?
// // because it is a promise - based library that uses async and await to interact with the database.
*/

function AboutPage() {
  return <div>AboutPage</div>;
}

export default AboutPage;
